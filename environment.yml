name: cookbook-dev
channels:
  - conda-forge
dependencies:
  - python=3.12
  - jupyterlab
  - mystmd
  - jupyterlab-myst
  # Note: OpenVisus not available on conda-forge currently; disable for now.
  #- OpenVisus==2.2.141
  - panel
  - bokeh
  - xmltodict
  - boto3
  - basemap
  - pelicanfs
